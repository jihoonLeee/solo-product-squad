tools:
  - name: file_read
    description: "기존 산출물 읽기 (mvp-scope.md)"
    parameters:
      path:
        type: string
        required: true
        description: "읽을 파일 경로"

  - name: file_write
    description: "UX 산출물 저장"
    parameters:
      path:
        type: string
        required: true
        description: "저장할 파일 경로 (output/ux/ia.md, wireframes.md, design-tokens.md)"
      content:
        type: string
        required: true
        description: "저장할 내용"
