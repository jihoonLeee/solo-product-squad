# solo-product-squad Gateway — 설치 매니페스트
# Setup 스킬이 이 파일을 읽어 도구 인프라를 구성함

# ─────────────────────────────────────────────
# MCP 서버
# ─────────────────────────────────────────────
mcp_servers:
  - name: context7
    description: "라이브러리 공식 문서 검색 및 코드 예시 제공 (frontend-engineer · backend-engineer 전용)"
    config: mcp/context7.json
    scope: user
    required: false   # context7 없이도 기본 워크플로우 동작

# ─────────────────────────────────────────────
# LSP 서버 — 플러그인 고유 LSP 없음 (OMC 빌트인으로 충분)
# ─────────────────────────────────────────────

# ─────────────────────────────────────────────
# 커스텀 도구 — 없음 (순수 에이전트+문서 기반)
# ─────────────────────────────────────────────
